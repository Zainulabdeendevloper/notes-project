<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit - <%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen flex flex-col items-center p-10">

  <!-- Back Button -->
  <a href="/" class="text-blue-400 hover:underline mb-6">&larr; Back to Home</a>

  <!-- Edit Form -->
  <div class="w-full max-w-2xl p-6 bg-zinc-800 rounded-lg shadow-lg">
    <h1 class="text-2xl font-bold mb-4">Edit Task: <%= title %></h1>

    <form action="/update/<%= filename %>" method="POST" class="flex flex-col space-y-4">
      
      <!-- Title (disabled so user cannot change filename) -->
      <input 
        type="text" 
        value="<%= title %>" 
        disabled
        class="px-4 py-2 rounded-md bg-zinc-700 text-gray-300 cursor-not-allowed"
      >

      <!-- Content -->
      <textarea 
        name="details" 
        rows="6" 
        class="px-4 py-2 rounded-md bg-zinc-700 focus:outline-none focus:ring-2 focus:ring-yellow-500 resize-none"
      ><%= content %></textarea>

      <!-- Save Button -->
      <button 
        type="submit" 
        class="bg-yellow-500 hover:bg-yellow-600 text-black px-5 py-2 rounded-md font-medium">
        Save Changes
      </button>
    </form>
  </div>

</body>
</html>
